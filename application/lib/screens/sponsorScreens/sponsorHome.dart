import 'package:flutter/material.dart';

class SponsorHome extends StatelessWidget {
  const SponsorHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Text("Happy!"));
  }
}
