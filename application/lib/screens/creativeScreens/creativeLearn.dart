import 'package:flutter/material.dart';

class CreativeLearn extends StatelessWidget {
  const CreativeLearn({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Text("Let us learn!"));
  }
}
